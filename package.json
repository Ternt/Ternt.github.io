{
  "name": "Portfolio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": " rsbuild",
    "preview": "rsbuild preview",
    "build-web": "rsbuild build",
    "build-bin": "call ./scripts/build.bat",
    "build-all": "rsbuild build --log-level silent && pnpm build-bin"
  },
  "devDependencies": {
    "@rsbuild/core": "^2.0.11"
  },
  "pnpm": {
    "silent": true
  }
}
